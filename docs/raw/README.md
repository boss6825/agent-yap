# `docs/raw/` — ungoverned staging ground

Dump raw signal and idea notes here untouched — call notes, support threads,
2am ideas. Nothing here is governed or trusted. Graduate an item into a Change
Request with `/signal-to-cr` (which dedupes and separates bugs from requests).
