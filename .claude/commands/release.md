# Release — TOMBSTONE

> This is not a skill. Releasing/publishing is a **human act** (C9).

## What to do instead
- Stage the ship artifacts and draft the PR (STOPS for human push/merge) →
  **`/ship`**.
- Generate the runbook + release notes from a spec, for an audited ✅ Done row only
  → **`/feature-to-release`**.

No skill pushes, merges, publishes, or deploys.
