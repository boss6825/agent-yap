# Deploy — TOMBSTONE

> This is not a skill. Agents never deploy (C9 — humans hold the trigger).

## What to do instead
- Prepare the ship artifacts (compliance entry, spec status, runbook, PR draft)
  → **`/ship`**. It STOPS for the human to push/merge; it never deploys.
- Generate the runbook + release notes for an audited ✅ Done feature →
  **`/feature-to-release`**.

Deployment itself is a **human act**. No skill performs it.
