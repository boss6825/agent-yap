# New-feature — TOMBSTONE

> This is not a skill. Feature work enters the Tier-4 pipeline at **`/define`**.

## What to do instead
- New behavior / "add" / "implement" → **`/define`** → `/spec` → `/plan` → `/red`
  → `/implement` → `/prove` → `/challenge` → `/check` → `/ship` →
  `/knowledge-sync`.
- If an approved PRD already exists in `docs/pm/prd/` → **`/prd-to-spec`**.
- If a spec already exists → skip to **`/plan`** (WORKFLOW Tier-4 spec gate).

See `specs/WORKFLOW.md` for the full pipeline.
