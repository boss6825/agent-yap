# Feature — TOMBSTONE

> This is not a skill. Enter the Tier-4 pipeline at **`/define`**.

## What to do instead
- New behavior / "add" / "implement" → **`/define`** (then the full pipeline; see
  `specs/WORKFLOW.md`).
- Approved PRD exists → **`/prd-to-spec`**.
- Spec exists → **`/plan`**.
- Product-level status of features → **`/feature-status`** (read-only) or
  **`/sprint-sync`** (reconcile).
