# Review — TOMBSTONE

> This is not a skill. Use the right review for the moment.

## What to do instead
- **Adversarial correctness/design review** of a change (severity-graded, Critical
  blocks merge) → **`/challenge`**.
- **Pre-code design review** (G3, the signed `review.md` gate; solo =
  adversarial agent in a fresh session + next-day self-sign) → part of **`/plan`**.
- The **deterministic** build/lint/constitution gate → **`/check`**.

There is no standalone `/review`.
